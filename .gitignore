ais_ids_pi/build
ais_ids_pi/po

# ── ml/ ────────────────────────────────────────────────────────────────
# 데이터 (실제 경로: D:\ais_data\)
ml/data/

# 비교 결과 (실제 경로: D:\ais_output\)
ml/output/

# 모델 파일 — train_benchmark.py 직접 실행 시 ml/ 루트에 생성될 수 있음
# (pipeline.py --base_dir 사용 시에는 D:\ais_models\ 로 저장)
ml/*.onnx
ml/*.json
ml/threshold*.txt
ml/eval_result_*.txt

# 전처리 중 생성되는 로그/임시 CSV
ml/*.csv

# PyTorch 체크포인트 (학습 중 임시 저장)
ml/*.pt
ml/*.pth

# 시각화 출력 (matplotlib 등)
ml/**/*.png
ml/**/*.jpg
ml/**/*.pdf

aivdm_gen/*.csv

# ── automation/ ─────────────────────────────────────────────────────────────
# 실제 환경변수 (절대 커밋 금지)
automation/.env

# Google Sheets 서비스 계정 JSON 키
automation/credentials.json

# MLflow 로컬 DB / 실험 디렉토리
mlflow.db
mlruns/
_test_mlruns/
_test_output/

# 시크릿 / 설정
ml/notify_config.json

# 런타임 생성 파일
ais_ids_alerts.log
*.log
ml/preprocess_progress.log
ml/preprocess_error.log

# Python 캐시
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.pytest_cache/

# Claude Code 설정
.claude/
