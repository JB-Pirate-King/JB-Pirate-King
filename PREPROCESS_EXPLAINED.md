# 🔍 preprocess.py - 뭘 하는 건가요?

## 📌 한 줄 요약

**AIS 항해 데이터를 ML 모델이 학습할 수 있도록 정제하고 특성을 만드는 전처리 스크립트**

---

## 🎯 목표

1. **원본 데이터 정제** - 오류나 이상값 제거
2. **특성 엔지니어링** - 8개의 새로운 특성(feature) 생성
3. **품질 보증** - 낮은 품질의 데이터 필터링
4. **학습 데이터 준비** - ML 모델이 바로 쓸 수 있는 형태로 변환

---

## 🔄 처리 순서

### 1️⃣ 입력 (Input)
```
CSV 파일: data/ais-2024-01-01.csv
내용: 항해 데이터 (위치, 속도, 방향, 시간 등)
```

### 2️⃣ 파싱 (Parsing)
- 라인별로 CSV 읽기
- 필요한 컬럼만 추출
  - `mmsi` - 선박 고유 ID
  - `latitude, longitude` - 위치
  - `sog` - 속도
  - `cog` - 진행 방향
  - `heading` - 선박 방향
  - `base_date_time` - 시간

### 3️⃣ 기본 검증
```
⚠️ 위도 범위 체크: -90 ≤ lat ≤ 90
⚠️ 경도 범위 체크: -180 ≤ lon ≤ 180
⚠️ 속도 체크: sog ≥ 0 (음수 제거)
```

### 4️⃣ 정렬 (Sorting)
```
1순위: MMSI (같은 선박끼리 그룹화)
2순위: 시간 (오래된 것부터)

결과: 선박별로 시간순 항해 경로
```

### 5️⃣ 결측값 처리
```
빈 값 → 이전 값으로 채우기 (Forward Fill)

예: [5.0, _, _, 8.0] → [5.0, 5.0, 5.0, 8.0]
```

### 6️⃣ 특성 생성 (Feature Engineering) ⭐
**8개의 새로운 특성 추가:**

#### 1. **dt** (시간 차이, 초)
```
현재 시간 - 이전 시간 = 몇 초 경과했는가?
```

#### 2. **dist_km** (거리, km)
```
이전 위치에서 현재 위치까지의 거리
공식: Haversine 거리 계산
```

#### 3. **cog_hdg_diff** (방향 차이, 도)
```
진행 방향(COG) - 선박 방향(Heading) = 얼마나 다른가?

정상: 거의 같음 (0도 근처)
비정상: 매우 다름 (30도 이상)
```

#### 4. **sog_change** (속도 변화, knot)
```
|현재 속도 - 이전 속도|

정상: 점진적 변화
비정상: 급격한 변화 (0에서 갑자기 25로)
```

#### 5. **cog_hdg_change** (방향 변화 속도)
```
cog_hdg_diff의 변화량

정상: 천천히 변화
비정상: 갑자기 변화 (위치 조작 신호)
```

#### 6. **speed_consistency** ⚠️ (중요)
```
실제 이동 거리 / SOG 기반 예상 거리

정상: ≈ 1.0 (보고된 속도로 맞게 이동)
비정상: > 1.5 (위치 조작, SOG 거짓 보고)

예: "속도 20 knot"이라고 하는데 실제로는 50km를 이동했다면?
→ 속도를 거짓으로 보고한 것!
```

#### 7. **lat_speed** (위도 변화율, 도/초)
```
(현재 위도 - 이전 위도) / dt

해석: 선박이 북쪽/남쪽으로 얼마나 빠르게 이동?
```

#### 8. **lon_speed** (경도 변화율, 도/초)
```
(현재 경도 - 이전 경도) / dt

해석: 선박이 동쪽/서쪽으로 얼마나 빠르게 이동?
```

---

### 7️⃣ 품질 필터링 (Quality Control)

**다음 중 하나라도 발견되면 해당 MMSI 모든 데이터 제거:**

#### ❌ 시퀀스 부족
```
데이터 포인트가 10개 미만
→ 신뢰할 만한 패턴 분석 불가
```

#### ❌ 이상값
```
위도/경도/속도 범위 위반
→ 데이터 기록 오류 또는 손상
```

#### ❌ 위치 점프 감지
```
만약:
  - 보고된 속도: 20 knot (약 37km/h)
  - 시간 경과: 1시간
  - 예상 거리: 37km
  
하지만:
  - 실제 거리: 100km (20배!)
  
→ 위치 조작 감지! 데이터 제거
```

---

### 8️⃣ 출력 (Output)

```
✅ ais_preprocessed.csv
   - 20개 컬럼 (기본 9개 + 파생 특성 8개 + mmsi, time)
   - ML 모델 학습에 바로 사용 가능

✅ ais_skip_log.csv
   - 제거된 선박(MMSI)과 제거 이유 기록
```

---

## 📊 처리 결과 예시

```
입력:      500,000행
↓ 파싱 검증
유효:      450,000행 (90%)
↓ 특성 생성
준비:      450,000행
↓ 품질 필터
최종 출력: 380,000행 (76% 유지)

제거됨:    120,000행 (24%)
제거 이유:
  - 시퀀스 부족: 45,000행
  - 이상값: 35,000행
  - 위치 점프: 40,000행
```

---

## 🎓 특성의 의미 (why?)

이 8개 특성은 **비정상 항해 패턴**을 감지하기 위한 것입니다:

| 특성 | 정상 신호 | 비정상 신호 |
|------|---------|----------|
| `dt` | 5~60초 | 0초 (같은 시간에 두 신호) |
| `dist_km` | SOG와 일치 | SOG와 심각한 불일치 |
| `cog_hdg_diff` | 5~15도 | 90도 이상 |
| `sog_change` | 0~2 knot | 5+ knot 급변 |
| `cog_hdg_change` | 천천히 | 급격하게 |
| `speed_consistency` | 0.8~1.2 | 1.5+ (위치 조작) |
| `lat_speed` | 범위 내 | 범위 외 |
| `lon_speed` | 범위 내 | 범위 외 |

**ML 모델은 이 8개 특성을 보고 "이건 정상" vs "이건 비정상"을 판단합니다!**

---

## 💡 예제 시나리오

### 정상 선박의 데이터
```
MMSI: 123456789 (일반 화물선)

시점 1: 2024-01-01 10:00:00
  위치: (35.1, 139.5)
  속도: 10 knot
  방향: 90도 (동쪽)

시점 2: 2024-01-01 10:10:00 (10분 후)
  위치: (35.1, 139.65)  ← 약 15km 동쪽으로 이동
  속도: 10 knot
  방향: 90도 (동쪽)

특성:
  dt = 600초
  dist_km = 15 (정상)
  speed_consistency = 1.0 (완벽 일치)
  
결과: ✅ 통과 → 학습 데이터 포함
```

### 비정상 선박의 데이터 (위치 조작)
```
MMSI: 987654321 (불법 어선)

시점 1: 2024-01-01 10:00:00
  위치: (35.1, 139.5)
  속도: 5 knot (느린 속도)
  방향: 90도

시점 2: 2024-01-01 10:10:00 (10분 후)
  위치: (35.1, 145.5)  ← 무려 700km 동쪽!!!
  속도: 5 knot (여전히 느림)
  방향: 90도

특성:
  dt = 600초
  dist_km = 700 (엄청남)
  speed_consistency = 60.0 (60배 차이!)
  
필터: 🚫 위치 점프 감지 → 모든 데이터 제거
```

---

## 🚀 실행

```bash
cd ml
python preprocess.py

# 또는 파일 지정
python preprocess.py data/ais-2024-01.csv
python preprocess.py data/
```

---

## ✅ 결론

**preprocess.py는 ML 모델이 "좋은 품질의 데이터"만 학습하도록 준비하는 역할입니다.**

1. 쓰레기 데이터 제거
2. 유용한 특성 생성
3. 학습 가능한 형태로 변환
4. 결과적으로 더 정확한 모델 학습

이게 없으면? → ML 모델이 쓸모없는 데이터로 학습 → 쓸모없는 예측 🚫

이게 있으면? → 깨끗한 데이터로 학습 → 정확한 비정상 탐지 ✅
